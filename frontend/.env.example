# Full URL of the Render API (no trailing slash)
# Leave empty locally to use the Vite proxy → backend on localhost:3001
VITE_API_BASE_URL=https://tanzeem-management-system-rawalpindi.onrender.com

# Optional local proxy target when VITE_API_BASE_URL is empty
# VITE_DEV_API_PROXY=http://localhost:3001
